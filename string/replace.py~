#!/usr/bin/python

string=raw_input("enter the string::")#string in which replacing is taking place
pos=raw_input("enter what is to be replace::")#string which is to be replace
rep=raw_input("enter the substring to replace::")#replacing string
print(string.replace(pos,rep))
